[build-system]
requires = ["setuptools>=64.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "moex_api"
version = "0.1.0"
authors = ['ENIAC_machine']
dependencies = [
		"numpy==2.4.4",
		"pandas==3.0.2",
		"scipy==1.17.1",
		"tqdm==4.67.3",
		"requests==2.33.1"
		]
